-- 우리 소유가 아닌 시트를 표시한다.
--
-- 그런 시트는 드라이브에서 부모 폴더가 없고 공유 문서함에 있을 뿐이라 우리 폴더로
-- 옮길 수 없다. 넣어 봐야 바로가기만 생기고 상대가 지우면 같이 사라진다.
-- 정리가 안 된 것이 아니라 정리할 수 없는 것이므로 화면에 그렇다고 알린다.
--
-- 설명란에 적지 않는 이유는 거기에 사람이 쓴 메모가 이미 들어 있기 때문이다.

ALTER TABLE "sheet_links" ADD COLUMN IF NOT EXISTS "externalOwner" TEXT;
